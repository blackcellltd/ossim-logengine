Copy this folder to the OSSIM AlienVault server. Set the logarchive server's IP in install.sh and run it.
allLogarchive.css contains every css. Could be changed to the standalone files (in comments) but first add <?php header("Content-type: text/css; charset: UTF-8");?> line to css file.
